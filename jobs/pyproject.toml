[project]
name = "terra-jobs"
version = "0.1.0"
description = "Background jobs for Terra"
requires-python = ">=3.12.4"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["jobs"]